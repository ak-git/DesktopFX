package com.ak.util;

import java.time.Duration;

public enum UIConstants {
  ;
  public static final Duration UI_DELAY_3SEC = Duration.ofSeconds(3);
  public static final Duration UI_DELAY_750MILLIS = Duration.ofMillis(750);
}